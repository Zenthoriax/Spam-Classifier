# 📧 Email Spam Classifier

A simple Machine Learning app that predicts if a message is **Spam** or **Ham** (Not Spam).

## 🛠️ Installation

1. **Install Dependencies**
   Run this command in your terminal:
   ```bash
   pip install -r requirements.txt
🚀 How to Run
Start the App

Bash

streamlit run app.py
Open in Browser

The app should open automatically.

If not, click the URL shown in the terminal (usually http://localhost:8501).

📂 Project Files
app.py: The main website code.

spam.csv: The dataset used for training.

requirements.txt: List of required libraries.

*.pkl: The saved AI models (Brain).
